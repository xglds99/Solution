public class l101isSymmetric {
    /**
     * 判断一棵树是否是轴对称树
     * @param root
     * @return
     */
    public boolean isSymmetric(TreeNode root){
        if (root == null)
            return true;

        return  false;
    }
}
